# build outputs
*.o
tfrpc

# test binaries and fuzzing artifacts
test/unit_tests
test/fuzz/*_fuzzer
*.profraw
*.profdata

# local cross-compilation toolchain (large, not part of the source)
openwrt-toolchain-*/

# editor/agent state
.codewhale

# release artifacts and the zig toolchain (local only)
dist/
zig-linux-x86_64-*/
zig-linux-x86_64-*.tar.xz

# local release helper
build.sh
